import { Card, CardH3, Meta } from "../ui";

export default function Correction() {
  return (
    <Card>
      <CardH3>Correction</CardH3>
      <Meta>
        Coming soon. Correcting a submitted application or attendance entry will require
        a PIN, held only by authorized staff, so every correction is attributable to a
        specific person.
      </Meta>
    </Card>
  );
}
